javascript:(() => { "use strict"; const INSTAGRAM_WEB_APP_ID = "936619743392459"; const INSTAGRAM_HOST_PATTERN = /(^|\.)instagram\.com$/i; const RESERVED_PATHS = new Set([ "about", "accounts", "api", "direct", "explore", "legal", "p", "reel", "reels", "stories", "web", ]); const DEFAULT_CONFIG = { targetUsername: "", relationshipPageSizes: [100, 50], relationshipPasses: 1, relationshipListDelayMs: 1100, exactSearchDelayMs: 1400, retryLimit: 5, retryBaseDelayMs: 6500, maxPagesPerPass: 250, exactSearchCount: 50, stopExactSearchOnAuthLost: true, }; const CONFIG = Object.assign( {}, DEFAULT_CONFIG, window.IG_OVER1K_CONFIG || {}, window.IG_FOLLOW_BACK_CONFIG || {}, ); if (!Array.isArray(CONFIG.relationshipPageSizes) || CONFIG.relationshipPageSizes.length === 0) { CONFIG.relationshipPageSizes = DEFAULT_CONFIG.relationshipPageSizes; } const sleep = (ms) => new Promise((resolve) => setTimeout(resolve, ms)); const normalizeUsername = (value) => String(value || "").trim().toLowerCase(); const formatNumber = (value) => ( typeof value === "number" && Number.isFinite(value) ? value.toLocaleString() : "unknown" ); const escapeHtml = (value) => String(value == null ? "" : value).replace(/[&<>"]/g, (char) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", "\"": "&quot;", }[char])); const jitter = (baseMs) => Math.round( baseMs + Math.random() * Math.min(2500, Math.max(300, baseMs / 3)), ); const state = { startedAt: new Date().toISOString(), phase: "starting", message: "Starting Instagram follow-back check...", logs: [], done: false, }; window.IG_FOLLOW_BACK_STATE = state; window.IG_OVER1K_STATE = state; class FetchProblem extends Error { constructor(message, details = {}) { super(message); Object.assign(this, details); } } function isAuthOrRateLimit(status, text) { const lowered = String(text || "").toLowerCase(); return ( status === 401 || status === 403 || (lowered.includes("login") && lowered.includes("required")) || lowered.includes("please wait a few minutes") ); } function progress(message, phase = state.phase) { state.phase = phase; state.message = message; state.logs.push({ at: new Date().toISOString(), message }); console.log(`[IG follow-back] ${message}`); renderProgress(); } function renderProgress() { let box = document.getElementById("ig-follow-back-progress-box"); if (!box) { box = document.createElement("div"); box.id = "ig-follow-back-progress-box"; Object.assign(box.style, { position: "fixed", zIndex: 2147483647, top: "14px", left: "14px", width: "min(520px, calc(100vw - 28px))", maxHeight: "70vh", overflow: "auto", color: "#f8fafc", background: "rgba(15, 23, 42, 0.96)", border: "1px solid rgba(148, 163, 184, 0.55)", borderRadius: "8px", padding: "14px 16px", font: "13px/1.45 -apple-system, BlinkMacSystemFont, Segoe UI, sans-serif", boxShadow: "0 18px 60px rgba(0, 0, 0, 0.35)", }); document.documentElement.appendChild(box); } const recentLogs = state.logs .slice(-6) .map((entry) => `<li>${escapeHtml(entry.message)}</li>`) .join(""); box.innerHTML = ` <div style="font-weight:700;font-size:15px;margin-bottom:6px;">IG follow-back checker</div> <div><strong>Phase:</strong> ${escapeHtml(state.phase)}</div> <div><strong>Status:</strong> ${escapeHtml(state.message)}</div> <ol style="margin:10px 0 0 18px;padding:0;">${recentLogs}</ol> `; } async function getJson(url, label) { let lastError = null; for (let attempt = 0; attempt <= CONFIG.retryLimit; attempt += 1) { try { const response = await fetch(url, { credentials: "include", headers: { accept: "application/json", "x-ig-app-id": INSTAGRAM_WEB_APP_ID, }, }); const text = await response.text(); if (isAuthOrRateLimit(response.status, text)) { throw new FetchProblem(`${label}: login or rate-limit wall (${response.status})`, { authLost: true, status: response.status, }); } if (!response.ok) { const retryAfter = response.headers?.get?.("retry-after"); const retryAfterNote = retryAfter ? ` Retry after ${retryAfter} seconds.` : ""; throw new FetchProblem(`${label}: ${response.status} ${response.statusText || ""}${retryAfterNote}`, { status: response.status, }); } try { return JSON.parse(text); } catch { throw new FetchProblem(`${label}: non-JSON Instagram response`, { status: response.status, }); } } catch (error) { lastError = error; if (error.authLost) { throw error; } if (attempt >= CONFIG.retryLimit) { break; } const waitMs = jitter(CONFIG.retryBaseDelayMs * (attempt + 1)); progress(`${label}: retry ${attempt + 1}/${CONFIG.retryLimit} after ${waitMs}ms`); await sleep(waitMs); } } throw lastError || new FetchProblem(`${label}: request failed`); } function assertInstagramOrigin() { const hostname = window.location.hostname || ""; if (!INSTAGRAM_HOST_PATTERN.test(hostname)) { throw new Error("Open instagram.com before running this script."); } } function currentPathUsername() { return window.location.pathname .split("/") .filter(Boolean)[0] ?.replace(/^@/, "") .trim() || ""; } function getTargetUsername() { const configuredUsername = CONFIG.targetUsername || window.IG_FOLLOW_BACK_USERNAME; if (configuredUsername) { return String(configuredUsername).replace(/^@/, "").trim(); } const username = currentPathUsername(); if ( username && /^[a-z0-9._]{1,30}$/i.test(username) && !RESERVED_PATHS.has(username.toLowerCase()) ) { return username; } return prompt("Instagram username to check:")?.replace(/^@/, "").trim(); } function profileCount(user, key) { const legacyEdge = key === "followers" ? user.edge_followed_by : user.edge_follow; const directKey = key === "followers" ? "follower_count" : "following_count"; if (typeof legacyEdge?.count === "number") { return legacyEdge.count; } if (typeof user[directKey] === "number") { return user[directKey]; } return null; } function toAccount(user) { return { username: user.username, fullName: user.full_name || user.fullName || "", id: String(user.pk || user.id || ""), isPrivate: Boolean(user.is_private), isVerified: Boolean(user.is_verified), }; } function formatAccount(account) { return account.fullName ? `@${account.username} - ${account.fullName}` : `@${account.username}`; } function extractUsers(response) { const rawUsers = response?.users || response?.items || response?.data?.users || []; return rawUsers .map((item) => item?.user || item?.node || item) .filter((user) => user && user.username); } function addUsers(targetMap, users) { let added = 0; for (const user of users) { const username = normalizeUsername(user.username); if (username && !targetMap.has(username)) { targetMap.set(username, toAccount(user)); added += 1; } } return added; } async function loadProfileUser(username) { progress(`Loading profile @${username}`, "profile"); const profile = await getJson( `/api/v1/users/web_profile_info/?username=${encodeURIComponent(username)}`, "profile", ); const user = profile?.data?.user || profile?.user; if (!user?.id && !user?.pk) { throw new Error(`Could not load profile data for @${username}. Instagram may have changed its response or the profile may be unavailable.`); } return { id: String(user.id || user.pk), username: user.username || username, fullName: user.full_name || "", followerCount: profileCount(user, "followers"), followingCount: profileCount(user, "following"), }; } async function loadRelationshipList(type, target) { const usersByUsername = new Map(); const passes = []; for (let pass = 1; pass <= CONFIG.relationshipPasses; pass += 1) { for (const pageSize of CONFIG.relationshipPageSizes) { let maxId = ""; let pageCount = 0; const before = usersByUsername.size; while (pageCount < CONFIG.maxPagesPerPass) { pageCount += 1; const cursor = maxId ? `&max_id=${encodeURIComponent(maxId)}` : ""; const response = await getJson( `/api/v1/friendships/${target.id}/${type}/?count=${encodeURIComponent(pageSize)}${cursor}`, `${type} page ${pageCount}`, ); const users = extractUsers(response); const added = addUsers(usersByUsername, users); progress( `${type} count ${pageSize}: page ${pageCount}, page users ${users.length}, added ${added}, union ${usersByUsername.size}`, type, ); const nextMaxId = response.next_max_id || response.nextMaxId || response?.page_info?.end_cursor || ""; if (!nextMaxId || response.has_more === false || users.length === 0) { break; } maxId = nextMaxId; await sleep(jitter(CONFIG.relationshipListDelayMs)); } passes.push({ kind: type, pass: `round${pass}`, count: pageSize, pages: pageCount, before, after: usersByUsername.size, added: usersByUsername.size - before, status: "ended", }); await sleep(jitter(CONFIG.relationshipListDelayMs * 2)); } } return { usersByUsername, passes, }; } async function exactFollowerSearch(target, username) { const encodedUsername = encodeURIComponent(username); const urls = [ `/api/v1/friendships/${target.id}/followers/?count=${CONFIG.exactSearchCount}&search_surface=follow_list_page&query=${encodedUsername}`, `/api/v1/friendships/${target.id}/followers/?count=${CONFIG.exactSearchCount}&query=${encodedUsername}`, ]; for (const url of urls) { const response = await getJson(url, `exact follower search @${username}`); const exactMatch = extractUsers(response).some( (user) => normalizeUsername(user.username) === normalizeUsername(username), ); if (exactMatch) { return true; } } return false; } function resultLines(results) { return results.length ? results.map((account) => `<li><a href="https://www.instagram.com/${escapeHtml(account.username)}/" target="_blank" rel="noreferrer">@${escapeHtml(account.username)}</a> ${escapeHtml(account.fullName)}</li>`).join("") : "<li>None</li>"; } function statCard(label, value) { return `<div class="card"><span>${escapeHtml(label)}</span><strong>${formatNumber(value)}</strong></div>`; } function renderFinalReport(result) { const warningItems = result.warnings.length ? result.warnings.map((warning) => `<li>${escapeHtml(warning)}</li>`).join("") : "<li>None</li>"; const passRows = result.loadPasses.map((pass) => ` <tr> <td>${escapeHtml(pass.kind)}</td> <td>${escapeHtml(pass.pass)}</td> <td>${escapeHtml(pass.count)}</td> <td>${escapeHtml(pass.pages)}</td> <td>${escapeHtml(pass.before)}</td> <td>${escapeHtml(pass.after)}</td> <td>${escapeHtml(pass.added)}</td> </tr> `).join(""); document.title = `Instagram follow-back result - @${result.target.username}`; document.body.innerHTML = ` <style> body { margin: 24px; background: #f8fafc; color: #0f172a; font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; } h1 { margin: 0 0 10px; font-size: 28px; } h2 { margin: 24px 0 10px; font-size: 20px; } p { max-width: 880px; } .grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(190px, 1fr)); gap: 10px; max-width: 1000px; } .card { background: white; border: 1px solid #dbe3ef; border-radius: 8px; padding: 14px; } .card span { display: block; color: #475569; font-size: 13px; } .card strong { display: block; margin-top: 5px; font-size: 23px; } .warn { max-width: 1000px; background: #fffbeb; border: 1px solid #f59e0b; border-radius: 8px; padding: 12px 14px; margin: 16px 0; } .cols { columns: 2 320px; } li { break-inside: avoid; margin: 5px 0; } a { color: #2563eb; text-decoration: none; } table { border-collapse: collapse; background: white; border: 1px solid #dbe3ef; } th, td { padding: 8px 10px; border: 1px solid #dbe3ef; text-align: left; } code { background: #e2e8f0; border-radius: 4px; padding: 2px 5px; } </style> <h1>Instagram follow-back result: @${escapeHtml(result.target.username)}</h1> <p>Only accounts in <strong>Verified not following back</strong> are counted as misses. Tentative misses corrected by exact follower search are removed, and failures or auth issues are kept in Unknown instead of being counted.</p> <div class="grid"> ${statCard("Profile following", result.profileCounts.following)} ${statCard("Loaded following", result.loaded.following)} ${statCard("Profile followers", result.profileCounts.followers)} ${statCard("Loaded followers", result.loaded.followers)} ${statCard("Tentative misses checked", result.tentativeMisses)} ${statCard("Verified not following back", result.verifiedNotFollowingBack.length)} ${statCard("Corrected by exact search", result.correctedByExactSearch.length)} ${statCard("Unknown, excluded", result.unknown.length)} </div> <div class="warn"><strong>Warnings</strong><ul>${warningItems}</ul></div> <h2>Verified not following back (${result.verifiedNotFollowingBack.length})</h2> <ol class="cols">${resultLines(result.verifiedNotFollowingBack)}</ol> <h2>Unknown - not counted (${result.unknown.length})</h2> <ol>${resultLines(result.unknown)}</ol> <h2>Corrected by exact follower search (${result.correctedByExactSearch.length})</h2> <ol class="cols">${resultLines(result.correctedByExactSearch)}</ol> <h2>Load passes</h2> <table> <thead><tr><th>Kind</th><th>Pass</th><th>Count</th><th>Pages</th><th>Before</th><th>After</th><th>Added</th></tr></thead> <tbody>${passRows}</tbody> </table> <p>Full structured results are in <code>window.IG_FOLLOW_BACK_RESULTS</code> and <code>window.IG_OVER1K_FOLLOW_BACK_RESULTS</code> until this page is reloaded.</p> `; } async function run() { assertInstagramOrigin(); const username = getTargetUsername(); if (!username) { throw new Error("Open an Instagram profile page or provide a username."); } const target = await loadProfileUser(username); progress( `Resolved @${target.username}: ${formatNumber(target.followerCount)} followers, ${formatNumber(target.followingCount)} following`, "profile", ); const followingLoad = await loadRelationshipList("following", target); const followerLoad = await loadRelationshipList("followers", target); const tentativeMisses = [...followingLoad.usersByUsername.values()] .filter((account) => !followerLoad.usersByUsername.has(normalizeUsername(account.username))) .sort((left, right) => left.username.localeCompare(right.username)); const verifiedNotFollowingBack = []; const correctedByExactSearch = []; const unknown = []; let authLost = false; progress( `Exact-checking ${tentativeMisses.length} tentative misses. Unknowns will not be counted as not following back.`, "exact verification", ); for (let index = 0; index < tentativeMisses.length; index += 1) { const account = tentativeMisses[index]; if (authLost && CONFIG.stopExactSearchOnAuthLost) { unknown.push({ ...account, reason: "Login or rate-limit wall appeared before exact search.", }); continue; } try { if (await exactFollowerSearch(target, account.username)) { correctedByExactSearch.push(account); } else { verifiedNotFollowingBack.push(account); } } catch (error) { if (error.authLost) { authLost = true; } unknown.push({ ...account, reason: error.message || String(error), }); } if ((index + 1) % 10 === 0 || index + 1 === tentativeMisses.length) { progress( `Exact checked ${index + 1}/${tentativeMisses.length}: corrected ${correctedByExactSearch.length}, verified missing ${verifiedNotFollowingBack.length}, unknown ${unknown.length}`, "exact verification", ); } await sleep(jitter(CONFIG.exactSearchDelayMs)); } const warnings = []; if ( typeof target.followingCount === "number" && followingLoad.usersByUsername.size < target.followingCount ) { warnings.push(`Bulk following list exposed ${followingLoad.usersByUsername.size} of ${target.followingCount}. The verified list has no known false positives, but it may miss not-followbacks among accounts Instagram did not expose.`); } if ( typeof target.followerCount === "number" && followerLoad.usersByUsername.size < target.followerCount ) { warnings.push(`Bulk followers list exposed ${followerLoad.usersByUsername.size} of ${target.followerCount}. Each tentative miss was exact-searched in followers; exact-search hits were corrected, and exact-search failures were moved to Unknown.`); } if (authLost) { warnings.push("Login or rate-limit wall appeared during exact verification; affected accounts were moved to Unknown instead of counted."); } const result = { generatedAt: new Date().toISOString(), target, profileCounts: { followers: target.followerCount, following: target.followingCount, }, loaded: { followers: followerLoad.usersByUsername.size, following: followingLoad.usersByUsername.size, }, tentativeMisses: tentativeMisses.length, verifiedNotFollowingBack, correctedByExactSearch, unknown, authLost, warnings, loadPasses: [...followingLoad.passes, ...followerLoad.passes], accuracyNote: "Only accounts in Verified not following back are counted as misses. Tentative misses corrected by exact follower search are removed, and failures/auth issues are kept in Unknown instead of being counted.", }; window.IG_FOLLOW_BACK_RESULTS = result; window.IG_OVER1K_FOLLOW_BACK_RESULTS = result; state.done = true; progress( `Done: ${verifiedNotFollowingBack.length} verified not following back, ${correctedByExactSearch.length} corrected, ${unknown.length} unknown.`, "done", ); console.log("IG_FOLLOW_BACK_RESULT_SUMMARY", { target: result.target.username, loaded: result.loaded, profileCounts: result.profileCounts, tentativeMisses: result.tentativeMisses, verifiedNotFollowingBack: result.verifiedNotFollowingBack.length, correctedByExactSearch: result.correctedByExactSearch.length, unknown: result.unknown.length, authLost: result.authLost, warnings: result.warnings, }); renderFinalReport(result); } run().catch((error) => { state.done = true; progress(error.message || String(error), "error"); console.error("Instagram follow-back checker failed", error); }); })();
