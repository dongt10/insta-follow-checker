javascript:(() => { "use strict"; const INSTAGRAM_WEB_APP_ID = "936619743392459"; const INSTAGRAM_HOST_PATTERN = /(^|\.)instagram\.com$/i; const RESERVED_PATHS = new Set([ "about", "accounts", "api", "direct", "explore", "legal", "p", "reel", "reels", "stories", "web", ]); const DEFAULT_CONFIG = { targetUsername: "", followerPageSize: 100, followingPageSize: 100, listDelayMs: 3500, minRequestIntervalMs: 1500, verifyMisses: "auto", verifyDelayMs: 4500, verifyMaxPages: 3, maxVerifications: 150, completenessTolerance: 0.99, retryLimit: 4, retryBaseDelayMs: 20000, retryMaxDelayMs: 300000, maxSlowdownFactor: 8, maxPagesPerList: 400, exactSearchCount: 50, resume: true, resumeTtlMs: 3600000, }; const CONFIG = Object.assign( {}, DEFAULT_CONFIG, window.IG_NON_FOLLOWERS_CONFIG || {}, window.IG_PUBLIC_CONFIG || {}, ); CONFIG.maxVerifications = Math.max(0, Math.floor(Number(CONFIG.maxVerifications) || 0)); const sleep = (ms) => new Promise((resolve) => setTimeout(resolve, ms)); const normalizeUsername = (value) => String(value || "").trim().toLowerCase(); const formatNumber = (value) => ( typeof value === "number" && Number.isFinite(value) ? value.toLocaleString() : "unknown" ); const escapeHtml = (value) => String(value == null ? "" : value).replace(/[&<>"]/g, (char) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", "\"": "&quot;", }[char])); const jitter = (baseMs) => Math.round( baseMs + Math.random() * Math.min(2500, Math.max(300, baseMs / 3)), ); const state = { startedAt: new Date().toISOString(), phase: "starting", message: "Starting Instagram non-follower check...", logs: [], requests: 0, walls: 0, done: false, }; window.IG_NON_FOLLOWERS_STATE = state; window.IG_PUBLIC_STATE = state; const pacing = { slowdownFactor: 1, cleanStreak: 0, lastRequestAt: 0, }; let resumeSaveWarned = false; function paceDelay(baseMs) { return jitter(baseMs * pacing.slowdownFactor); } function reportWall() { state.walls += 1; pacing.cleanStreak = 0; if (pacing.slowdownFactor < CONFIG.maxSlowdownFactor) { pacing.slowdownFactor = Math.min(CONFIG.maxSlowdownFactor, pacing.slowdownFactor * 2); progress(`Instagram is pushing back: slowing all requests to ${pacing.slowdownFactor}x spacing.`); } } function reportCleanResponse() { pacing.cleanStreak += 1; if (pacing.cleanStreak >= 25 && pacing.slowdownFactor > 1) { pacing.slowdownFactor = Math.max(1, pacing.slowdownFactor / 2); pacing.cleanStreak = 0; } } async function throttleBeforeRequest() { const minIntervalMs = CONFIG.minRequestIntervalMs * pacing.slowdownFactor; const waitMs = pacing.lastRequestAt + minIntervalMs - Date.now(); if (waitMs > 0) { await sleep(waitMs); } pacing.lastRequestAt = Date.now(); } function getCookie(name) { try { const match = String(document.cookie || "").match( new RegExp(`(?:^|;\\s*)${name}=([^;]+)`), ); return match ? decodeURIComponent(match[1]) : ""; } catch { return ""; } } class FetchProblem extends Error { constructor(message, details = {}) { super(message); Object.assign(this, details); } } function looksLikeHtml(text) { const trimmed = String(text || "").trim().slice(0, 500).toLowerCase(); return trimmed.startsWith("<!doctype html") || trimmed.startsWith("<html") || trimmed.includes("<head") || trimmed.includes("<body"); } const RATE_PHRASES = [ "please wait a few minutes", "temporarily blocked", "feedback_required", "we limit how often", "restrict certain activity", "we restrict certain activity", "try again later", "action blocked", "automated behavior", "please wait a few moments", ]; function includesRatePhrase(text) { return RATE_PHRASES.some((phrase) => text.includes(phrase)); } function jsonBlockReason(status, parsed) { const message = `${String(parsed?.message || "")} ${String(parsed?.feedback_message || "")} ${String(parsed?.title || "")}`.toLowerCase(); if ( status === 401 || status === 403 || message.includes("login_required") || message.includes("challenge_required") ) { return "auth"; } if ( status === 429 || parsed?.spam === true || String(parsed?.feedback_action || "").length > 0 || includesRatePhrase(message) ) { return "rate"; } return ""; } function rawBlockReason(status, text, contentType = "") { const lowered = String(text || "").toLowerCase(); const loweredContentType = String(contentType || "").toLowerCase(); if ( status === 401 || status === 403 || lowered.includes("login_required") || lowered.includes("challenge_required") ) { return "auth"; } if (status === 429 || includesRatePhrase(lowered)) { return "rate"; } if (lowered.includes("login") && lowered.includes("required")) { return "auth"; } if (loweredContentType.includes("text/html") || looksLikeHtml(text)) { return "html"; } return ""; } function parseRetryAfterMs(response) { const header = response.headers?.get?.("retry-after"); const headerText = header == null ? "" : String(header).trim(); if (!headerText) { return 0; } const seconds = Number(headerText); if (Number.isFinite(seconds) && seconds > 0) { return seconds * 1000; } const dateMs = Date.parse(headerText) - Date.now(); return Number.isFinite(dateMs) && dateMs > 0 ? dateMs : 0; } function progress(message, phase = state.phase) { state.phase = phase; state.message = message; state.logs.push({ at: new Date().toISOString(), message }); console.log(`[IG non-followers] ${message}`); renderProgress(); } function renderProgress() { let box = document.getElementById("ig-non-followers-progress-box"); if (!box) { box = document.createElement("div"); box.id = "ig-non-followers-progress-box"; Object.assign(box.style, { position: "fixed", zIndex: 2147483647, top: "14px", left: "14px", width: "min(520px, calc(100vw - 28px))", maxHeight: "70vh", overflow: "auto", color: "#f8fafc", background: "rgba(15, 23, 42, 0.96)", border: "1px solid rgba(148, 163, 184, 0.55)", borderRadius: "8px", padding: "14px 16px", font: "13px/1.45 -apple-system, BlinkMacSystemFont, Segoe UI, sans-serif", boxShadow: "0 18px 60px rgba(0, 0, 0, 0.35)", }); document.documentElement.appendChild(box); } const recentLogs = state.logs .slice(-6) .map((entry) => `<li>${escapeHtml(entry.message)}</li>`) .join(""); box.innerHTML = ` <div style="font-weight:700;font-size:15px;margin-bottom:6px;">IG non-follower checker (public)</div> <div><strong>Phase:</strong> ${escapeHtml(state.phase)}</div> <div><strong>Status:</strong> ${escapeHtml(state.message)}</div> <div><strong>Requests:</strong> ${escapeHtml(state.requests)} | <strong>Pacing:</strong> ${escapeHtml(pacing.slowdownFactor)}x</div> <ol style="margin:10px 0 0 18px;padding:0;">${recentLogs}</ol> `; } async function getJson(url, label) { let lastError = null; for (let attempt = 0; attempt <= CONFIG.retryLimit; attempt += 1) { try { await throttleBeforeRequest(); state.requests += 1; const response = await fetch(url, { credentials: "include", headers: { accept: "application/json", "x-ig-app-id": INSTAGRAM_WEB_APP_ID, }, }); const text = await response.text(); const contentType = response.headers?.get?.("content-type") || ""; const retryAfterMs = parseRetryAfterMs(response); let parsed = null; let parsedOk = false; try { parsed = JSON.parse(text); parsedOk = true; } catch { parsedOk = false; } const blockReason = parsedOk ? jsonBlockReason(response.status, parsed) : rawBlockReason(response.status, text, contentType); if (blockReason) { const blockLabel = { auth: "login", rate: "rate-limit / action block", html: "HTML/non-JSON", }[blockReason]; throw new FetchProblem(`${label}: ${blockLabel} wall (${response.status})`, { authLost: blockReason === "auth", rateLimited: blockReason === "rate", relationshipBlocked: blockReason === "html", status: response.status, contentType, retryAfterMs, }); } if (!response.ok) { const retryAfterNote = retryAfterMs ? ` Retry after ${Math.round(retryAfterMs / 1000)} seconds.` : ""; throw new FetchProblem(`${label}: ${response.status} ${response.statusText || ""}${retryAfterNote}`, { status: response.status, retryAfterMs, }); } if (parsedOk) { reportCleanResponse(); return parsed; } throw new FetchProblem(`${label}: non-JSON Instagram response`, { status: response.status, contentType, }); } catch (error) { lastError = error; if (error.rateLimited || error.relationshipBlocked) { reportWall(); } if (error.authLost && !error.rateLimited) { throw error; } if ( typeof error.status === "number" && error.status >= 400 && error.status < 500 && !error.rateLimited && !error.relationshipBlocked ) { throw error; } if (attempt >= CONFIG.retryLimit) { break; } const backoffMs = error.rateLimited ? CONFIG.retryBaseDelayMs * (2 ** attempt) : CONFIG.retryBaseDelayMs * (attempt + 1); const waitMs = Math.max( Math.min(CONFIG.retryMaxDelayMs, jitter(backoffMs)), error.retryAfterMs || 0, ); progress(`${label}: retry ${attempt + 1}/${CONFIG.retryLimit} after ${Math.round(waitMs / 1000)}s`); await sleep(waitMs); } } throw lastError || new FetchProblem(`${label}: request failed`); } function assertInstagramOrigin() { const hostname = window.location.hostname || ""; if (!INSTAGRAM_HOST_PATTERN.test(hostname)) { throw new Error("Open instagram.com before running this script."); } } function currentPathUsername() { return window.location.pathname .split("/") .filter(Boolean)[0] ?.replace(/^@/, "") .trim() || ""; } function getTargetUsername() { const configuredUsername = CONFIG.targetUsername || window.IG_NON_FOLLOWERS_USERNAME; if (configuredUsername) { return String(configuredUsername).replace(/^@/, "").trim(); } const username = currentPathUsername(); if ( username && /^[a-z0-9._]{1,30}$/i.test(username) && !RESERVED_PATHS.has(username.toLowerCase()) ) { return username; } return prompt("Instagram username to check (the account you are viewing):")?.replace(/^@/, "").trim(); } function profileCount(user, key) { const legacyEdge = key === "followers" ? user.edge_followed_by : user.edge_follow; const directKey = key === "followers" ? "follower_count" : "following_count"; if (typeof legacyEdge?.count === "number") { return legacyEdge.count; } if (typeof user[directKey] === "number") { return user[directKey]; } return null; } function toAccount(user) { return { username: user.username, fullName: user.full_name || user.fullName || "", id: String(user.pk || user.id || ""), isPrivate: Boolean(user.is_private ?? user.isPrivate), isVerified: Boolean(user.is_verified ?? user.isVerified), }; } function extractUsers(response) { const rawUsers = response?.users || response?.items || response?.data?.users || []; return rawUsers .map((item) => item?.user || item?.node || item) .filter((user) => user && user.username); } function addUsers(targetMap, users) { let added = 0; for (const user of users) { const username = normalizeUsername(user.username); if (username && !targetMap.has(username)) { targetMap.set(username, toAccount(user)); added += 1; } } return added; } function resumeKey(targetId) { return `ig-non-followers-resume:${getCookie("ds_user_id") || "anon"}:${targetId}`; } function verdictKey(account) { return `v:${account.id || normalizeUsername(account.username)}`; } function clearResumeState(targetId) { try { window.localStorage?.removeItem?.(resumeKey(targetId)); } catch { return; } } function loadResumeState(targetId) { const empty = { createdAt: Date.now(), lists: {}, verdicts: {}, loadedFromStorage: false, ageMinutes: 0, }; if (!CONFIG.resume) { clearResumeState(targetId); return empty; } try { const raw = window.localStorage ? window.localStorage.getItem(resumeKey(targetId)) : null; if (!raw) { return empty; } const saved = JSON.parse(raw); if ( !saved || typeof saved.createdAt !== "number" || Date.now() - saved.createdAt > CONFIG.resumeTtlMs ) { clearResumeState(targetId); return empty; } return { createdAt: saved.createdAt, lists: saved.lists && typeof saved.lists === "object" ? saved.lists : {}, verdicts: saved.verdicts && typeof saved.verdicts === "object" ? saved.verdicts : {}, loadedFromStorage: true, ageMinutes: Math.max(0, Math.round((Date.now() - saved.createdAt) / 60000)), }; } catch { clearResumeState(targetId); return empty; } } function saveResumeState(targetId, resume) { if (!CONFIG.resume) { return; } const payloads = [ { createdAt: resume.createdAt, lists: resume.lists, verdicts: resume.verdicts }, { createdAt: resume.createdAt, lists: {}, verdicts: resume.verdicts }, ]; for (const payload of payloads) { try { window.localStorage?.setItem?.(resumeKey(targetId), JSON.stringify(payload)); return; } catch { continue; } } if (!resumeSaveWarned) { resumeSaveWarned = true; progress("Could not save resume progress (browser storage full or unavailable); a rerun would start fresh."); } } async function loadProfileUser(username) { progress(`Loading profile @${username}`, "profile"); const profile = await getJson( `/api/v1/users/web_profile_info/?username=${encodeURIComponent(username)}`, "profile", ); const user = profile?.data?.user || profile?.user; if (!user?.id && !user?.pk) { throw new Error(`Could not load profile data for @${username}. The profile may be unavailable, or Instagram changed its response.`); } return { id: String(user.id || user.pk), username: user.username || username, fullName: user.full_name || "", isPrivate: Boolean(user.is_private), followedByViewer: Boolean(user.followed_by_viewer), followerCount: profileCount(user, "followers"), followingCount: profileCount(user, "following"), }; } async function loadRelationshipList(type, target, resume) { const expectedCount = type === "followers" ? target.followerCount : target.followingCount; const pageSize = type === "followers" ? CONFIG.followerPageSize : CONFIG.followingPageSize; const savedList = resume.lists[type]; const savedAccounts = Array.isArray(savedList?.accounts) ? savedList.accounts.filter((account) => account && typeof account.username === "string" && account.username) : []; const usersByUsername = new Map(); const passes = []; if ( savedList?.complete && savedAccounts.length > 0 && (typeof expectedCount !== "number" || savedAccounts.length >= expectedCount) ) { addUsers(usersByUsername, savedAccounts); progress( `Reusing the ${type} list saved ${resume.ageMinutes} minutes ago (${usersByUsername.size} accounts), no new requests needed.`, type, ); return { usersByUsername, passes: [{ kind: type, pass: "resume", count: "-", pages: 0, before: 0, after: usersByUsername.size, added: usersByUsername.size, status: "resumed", }], stoppedEarly: false, stopReason: "", stopStatus: "", reachedEnd: true, }; } let maxId = ""; let seededFromCursor = false; if (savedAccounts.length > 0) { addUsers(usersByUsername, savedAccounts); if (!savedList.complete && savedList.maxId) { maxId = String(savedList.maxId); seededFromCursor = true; } progress( `Seeding the ${type} list with ${usersByUsername.size} accounts saved ${resume.ageMinutes} minutes ago${seededFromCursor ? ", continuing from the saved position" : ""}.`, type, ); } const before = usersByUsername.size; let pageCount = 0; let reachedEnd = false; const savePartial = () => { if (!CONFIG.resume) { return; } resume.lists[type] = { complete: false, accounts: [...usersByUsername.values()], maxId: maxId || "", pageSize, }; saveResumeState(target.id, resume); }; while (pageCount < CONFIG.maxPagesPerList) { pageCount += 1; const cursorParam = maxId ? `&max_id=${encodeURIComponent(maxId)}` : ""; let response; try { response = await getJson( `/api/v1/friendships/${target.id}/${type}/?count=${encodeURIComponent(pageSize)}${cursorParam}`, `${type} page ${pageCount}`, ); } catch (error) { if (!error.authLost && !error.rateLimited && !error.relationshipBlocked) { if (seededFromCursor && pageCount === 1) { progress(`${type}: the saved resume position was rejected, restarting this list from the beginning.`, type); seededFromCursor = false; maxId = ""; pageCount = 0; continue; } throw error; } const stopReason = error.message || String(error); const stopStatus = error.rateLimited ? "rate-limited" : error.relationshipBlocked ? "html-blocked" : "auth-blocked"; progress(`${type} stopped early: ${stopReason}`, type); passes.push({ kind: type, pass: "round1", count: pageSize, pages: pageCount, before, after: usersByUsername.size, added: usersByUsername.size - before, status: stopStatus, }); savePartial(); return { usersByUsername, passes, stoppedEarly: true, stopReason, stopStatus, reachedEnd: false, }; } const users = extractUsers(response); const added = addUsers(usersByUsername, users); progress( `${type}: page ${pageCount}, page users ${users.length}, added ${added}, total ${usersByUsername.size}`, type, ); const nextMaxId = response.next_max_id || response.nextMaxId || response?.page_info?.end_cursor || ""; if (!nextMaxId || response.has_more === false || users.length === 0) { reachedEnd = true; break; } maxId = nextMaxId; await sleep(paceDelay(CONFIG.listDelayMs)); } passes.push({ kind: type, pass: "round1", count: pageSize, pages: pageCount, before, after: usersByUsername.size, added: usersByUsername.size - before, status: reachedEnd ? "ended" : "page-cap", }); if (CONFIG.resume) { resume.lists[type] = { complete: reachedEnd && (typeof expectedCount !== "number" || usersByUsername.size >= expectedCount), accounts: [...usersByUsername.values()], maxId: "", pageSize, }; saveResumeState(target.id, resume); } return { usersByUsername, passes, stoppedEarly: false, stopReason: "", stopStatus: "", reachedEnd, }; } async function exactFollowerSearch(target, username) { const encodedUsername = encodeURIComponent(username); const baseUrls = [ `/api/v1/friendships/${target.id}/followers/?count=${CONFIG.exactSearchCount}&search_surface=follow_list_page&query=${encodedUsername}`, `/api/v1/friendships/${target.id}/followers/?count=${CONFIG.exactSearchCount}&query=${encodedUsername}`, ]; let truncated = false; for (const baseUrl of baseUrls) { let maxId = ""; for (let page = 1; page <= CONFIG.verifyMaxPages; page += 1) { const cursorParam = maxId ? `&max_id=${encodeURIComponent(maxId)}` : ""; const response = await getJson(`${baseUrl}${cursorParam}`, `exact follower search @${username}`); const users = extractUsers(response); const exactMatch = users.some( (user) => normalizeUsername(user.username) === normalizeUsername(username), ); if (exactMatch) { return true; } const nextMaxId = response.next_max_id || response.nextMaxId || ""; if (!nextMaxId || response.has_more === false || users.length === 0) { break; } if (page === CONFIG.verifyMaxPages) { truncated = true; break; } maxId = nextMaxId; await sleep(paceDelay(CONFIG.verifyDelayMs)); } } if (truncated) { throw new FetchProblem(`exact follower search @${username}: too many similar usernames to check definitively`, { searchTruncated: true, }); } return false; } function resultLines(results) { return results.length ? results.map((account) => `<li><a href="https://www.instagram.com/${escapeHtml(account.username)}/" target="_blank" rel="noreferrer">@${escapeHtml(account.username)}</a> ${escapeHtml(account.fullName)}</li>`).join("") : "<li>None</li>"; } function statCard(label, value) { const display = typeof value === "number" ? formatNumber(value) : (value || "unknown"); return `<div class="card"><span>${escapeHtml(label)}</span><strong>${escapeHtml(display)}</strong></div>`; } function section(title, accounts, columns) { return `<h2>${escapeHtml(title)} (${accounts.length})</h2><ol class="${columns ? "cols" : ""}">${resultLines(accounts)}</ol>`; } function renderFinalReport(result) { const warningItems = result.warnings.length ? result.warnings.map((warning) => `<li>${escapeHtml(warning)}</li>`).join("") : "<li>None</li>"; const passRows = result.loadPasses.map((pass) => ` <tr> <td>${escapeHtml(pass.kind)}</td> <td>${escapeHtml(pass.pass)}</td> <td>${escapeHtml(pass.count)}</td> <td>${escapeHtml(pass.pages)}</td> <td>${escapeHtml(pass.after)}</td> <td>${escapeHtml(pass.added)}</td> <td>${escapeHtml(pass.status)}</td> </tr> `).join(""); const unconfirmedSection = result.unconfirmed.length ? section("Not following back - unconfirmed", result.unconfirmed, true) : ""; const rescuedSection = result.rescued.length ? section("Actually follows back - removed from misses", result.rescued, true) : ""; const unknownSection = result.unknown.length ? section("Unknown - verification blocked", result.unknown, false) : ""; const bonusSection = result.fansNotFollowedBack ? section("Bonus: followers you (the target) do not follow back", result.fansNotFollowedBack, true) : ""; document.getElementById("ig-non-followers-progress-box")?.remove?.(); document.title = `Instagram non-followers - @${result.target.username}`; document.body.innerHTML = ` <style> body { margin: 24px; background: #f8fafc; color: #0f172a; font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; } h1 { margin: 0 0 10px; font-size: 28px; } h2 { margin: 24px 0 10px; font-size: 20px; } p { max-width: 880px; } .grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(190px, 1fr)); gap: 10px; max-width: 1000px; } .card { background: white; border: 1px solid #dbe3ef; border-radius: 8px; padding: 14px; } .card span { display: block; color: #475569; font-size: 13px; } .card strong { display: block; margin-top: 5px; font-size: 23px; } .warn { max-width: 1000px; background: #fffbeb; border: 1px solid #f59e0b; border-radius: 8px; padding: 12px 14px; margin: 16px 0; } .cols { columns: 2 320px; } li { break-inside: avoid; margin: 5px 0; } a { color: #2563eb; text-decoration: none; } table { border-collapse: collapse; background: white; border: 1px solid #dbe3ef; } th, td { padding: 8px 10px; border: 1px solid #dbe3ef; text-align: left; } code { background: #e2e8f0; border-radius: 4px; padding: 2px 5px; } </style> <h1>Instagram non-followers: @${escapeHtml(result.target.username)}</h1> <p>Accounts @${escapeHtml(result.target.username)} follows that do <strong>not</strong> follow back. "Confirmed" is exact: the follower list was read completely, so the difference is definitive. ${result.unconfirmed.length ? "\"Unconfirmed\" accounts could not be proven because Instagram did not expose the full follower list - rerun later to confirm them." : ""}</p> <p><strong>Follower list read:</strong> ${escapeHtml(result.followerListComplete ? "complete (exact result)" : "incomplete (some accounts unconfirmed)")} | <strong>Verification:</strong> ${escapeHtml(result.verificationMode)}</p> <div class="grid"> ${statCard("Profile following", result.profileCounts.following)} ${statCard("Loaded following", result.loaded.following)} ${statCard("Profile followers", result.profileCounts.followers)} ${statCard("Loaded followers", result.loaded.followers)} ${statCard("Not following back (confirmed)", result.confirmed.length)} ${statCard("Unconfirmed", result.unconfirmed.length)} ${statCard("Follows back (removed)", result.rescued.length)} ${statCard("Unknown", result.unknown.length)} ${statCard("Requests made", result.requestsMade)} </div> <div class="warn"><strong>Warnings</strong><ul>${warningItems}</ul></div> ${section("Not following back - confirmed", result.confirmed, true)} ${unconfirmedSection} ${rescuedSection} ${unknownSection} ${bonusSection} <h2>Load passes</h2> <table> <thead><tr><th>Kind</th><th>Pass</th><th>Count</th><th>Pages</th><th>Total</th><th>Added</th><th>Status</th></tr></thead> <tbody>${passRows}</tbody> </table> <p>Full structured results are in <code>window.IG_NON_FOLLOWERS_RESULTS</code> until this page is reloaded.</p> `; } async function run() { assertInstagramOrigin(); const username = getTargetUsername(); if (!username) { throw new Error("Open an Instagram profile page or provide a username."); } const target = await loadProfileUser(username); progress( `Resolved @${target.username}: ${formatNumber(target.followerCount)} followers, ${formatNumber(target.followingCount)} following`, "profile", ); const warnings = []; if (getCookie("ds_user_id") && getCookie("ds_user_id") === target.id) { warnings.push("This is your own account. check-follow-back.js is faster for your own account because it can use Instagram's batch friendship endpoint; this public script still works but reads more pages."); } if (target.isPrivate && !target.followedByViewer) { throw new Error(`@${target.username} is private and you do not follow them, so Instagram will not expose their follower/following lists. Follow them (or use an account that does) and rerun.`); } const resume = loadResumeState(target.id); if ( resume.loadedFromStorage && (Object.keys(resume.lists).length > 0 || Object.keys(resume.verdicts).length > 0) ) { warnings.push(`Reused saved progress from ${resume.ageMinutes} minutes ago to avoid repeating requests. For a fully fresh scan, run with window.IG_NON_FOLLOWERS_CONFIG = { resume: false }.`); } const followingLoad = await loadRelationshipList("following", target, resume); const followingListUnavailable = ( followingLoad.stoppedEarly && followingLoad.usersByUsername.size === 0 && (typeof target.followingCount !== "number" || target.followingCount > 0) ); let followerLoad; if (followingListUnavailable) { progress( "Skipping the follower list: the following list was blocked before any accounts loaded, so this run cannot produce a result anyway.", "followers", ); followerLoad = { usersByUsername: new Map(), passes: [{ kind: "followers", pass: "skipped", count: "-", pages: 0, before: 0, after: 0, added: 0, status: "skipped (following blocked)" }], stoppedEarly: false, stopReason: "", stopStatus: "", reachedEnd: false, skipped: true, }; } else { followerLoad = await loadRelationshipList("followers", target, resume); } const followerListUnavailable = ( followerLoad.stoppedEarly && followerLoad.usersByUsername.size === 0 && (typeof target.followerCount !== "number" || target.followerCount > 0) ); const confirmed = []; const unconfirmed = []; const rescued = []; const unknown = []; let verificationMode = "none (complete follower list, exact difference)"; let followerListComplete = false; let authLost = false; if (followingListUnavailable || followerListUnavailable) { progress( "Stopped safely because Instagram blocked a required list before enough data loaded. No accounts were counted.", "blocked", ); verificationMode = "stopped before verification"; } else { const tentativeMisses = [...followingLoad.usersByUsername.values()] .filter((account) => !followerLoad.usersByUsername.has(normalizeUsername(account.username))) .sort((left, right) => left.username.localeCompare(right.username)); followerListComplete = Boolean( followerLoad.reachedEnd && ( typeof target.followerCount !== "number" || followerLoad.usersByUsername.size >= target.followerCount * CONFIG.completenessTolerance ), ); const shouldVerify = tentativeMisses.length > 0 && ( CONFIG.verifyMisses === true || (CONFIG.verifyMisses === "auto" && !followerListComplete) ); if (!shouldVerify) { if (followerListComplete) { confirmed.push(...tentativeMisses); verificationMode = "none needed (follower list complete)"; } else { unconfirmed.push(...tentativeMisses); verificationMode = CONFIG.verifyMisses === false ? "skipped by config (verifyMisses=false)" : "skipped"; } } else { verificationMode = "exact follower search (paced, abort-on-block)"; const recordVerdict = (account, followsBack) => { (followsBack ? rescued : confirmed).push(account); resume.verdicts[verdictKey(account)] = { followsBack }; }; const pending = []; let resumedVerdicts = 0; for (const account of tentativeMisses) { const savedVerdict = resume.verdicts[verdictKey(account)]; if (savedVerdict && typeof savedVerdict.followsBack === "boolean") { (savedVerdict.followsBack ? rescued : confirmed).push(account); resumedVerdicts += 1; } else { pending.push(account); } } if (resumedVerdicts > 0) { progress(`Reused ${resumedVerdicts} verified results from the previous run, ${pending.length} still to check.`, "verification"); } let toVerify = pending; if (pending.length > 0 && followerLoad.usersByUsername.size > 0) { const canary = followerLoad.usersByUsername.values().next().value; let canaryProblem = ""; try { if (!(await exactFollowerSearch(target, canary.username))) { canaryProblem = `Exact follower search could not find @${canary.username}, a known follower, so search results are not reliable right now.`; } } catch (error) { if (error.authLost || error.rateLimited || error.relationshipBlocked) { authLost = true; } canaryProblem = `Exact follower search reliability check failed: ${error.message || String(error)}`; } if (canaryProblem) { progress(canaryProblem, "verification"); warnings.push(`${canaryProblem} Accounts that could not be confirmed were left as Unconfirmed instead of being counted. Wait 15-30 minutes and rerun.`); unconfirmed.push(...pending); toVerify = []; } else { await sleep(paceDelay(CONFIG.verifyDelayMs)); } } if (toVerify.length > CONFIG.maxVerifications) { const overflow = toVerify.slice(CONFIG.maxVerifications); unconfirmed.push(...overflow); warnings.push(`${overflow.length} accounts were left Unconfirmed to stay under the ${CONFIG.maxVerifications}-verification safety cap. Rerun later to confirm them; saved progress means the rerun continues where this one stopped.`); toVerify = toVerify.slice(0, CONFIG.maxVerifications); } for (let index = 0; index < toVerify.length; index += 1) { const account = toVerify[index]; if (authLost) { unconfirmed.push(account); continue; } try { recordVerdict(account, await exactFollowerSearch(target, account.username)); } catch (error) { if (error.authLost || error.rateLimited || error.relationshipBlocked) { authLost = true; unknown.push({ ...account, reason: error.message || String(error) }); } else if (error.searchTruncated) { unconfirmed.push(account); } else { unknown.push({ ...account, reason: error.message || String(error) }); } } if ((index + 1) % 5 === 0 || index + 1 === toVerify.length) { progress( `Verified ${index + 1}/${toVerify.length}: confirmed ${confirmed.length}, follows back ${rescued.length}, unconfirmed ${unconfirmed.length}, unknown ${unknown.length}`, "verification", ); saveResumeState(target.id, resume); } if (index + 1 < toVerify.length && !authLost) { await sleep(paceDelay(CONFIG.verifyDelayMs)); } } } } let fansNotFollowedBack = null; if ( !followingListUnavailable && !followerListUnavailable && followingLoad.reachedEnd && followerLoad.reachedEnd && !followerLoad.skipped ) { fansNotFollowedBack = [...followerLoad.usersByUsername.values()] .filter((account) => !followingLoad.usersByUsername.has(normalizeUsername(account.username))) .sort((left, right) => left.username.localeCompare(right.username)); } if (followingLoad.stoppedEarly) { warnings.push(`Following list stopped early: ${followingLoad.stopReason}. Wait 15-30 minutes and rerun; saved progress is reused so the rerun only requests what is still missing.`); } if (followerLoad.stoppedEarly) { warnings.push(`Followers list stopped early: ${followerLoad.stopReason}. No false positives were counted from the blocked data.`); } if (followingListUnavailable || followerListUnavailable) { warnings.push("No reliable result was produced because Instagram blocked a required list. This is the action-block Instagram shows when it sees automated list loading. Wait 15-30 minutes, reopen the profile, and rerun: the script reuses saved progress and paces itself more slowly after a block."); } else if (!followerListComplete && !authLost) { warnings.push(`The follower list could not be read in full (loaded ${followerLoad.usersByUsername.size} of ${formatNumber(target.followerCount)}). Confirmed accounts are still exact; Unconfirmed accounts may actually follow back. Rerun later to resolve them.`); } if (authLost) { warnings.push("An action block appeared during verification, so remaining accounts were left Unconfirmed rather than risk a longer block. Wait 15-30 minutes and rerun: verified results were saved."); } const cleanRun = ( !followingLoad.stoppedEarly && !followerLoad.stoppedEarly && !authLost && unknown.length === 0 && unconfirmed.length === 0 ); if (cleanRun) { clearResumeState(target.id); } else { saveResumeState(target.id, resume); } confirmed.sort((left, right) => left.username.localeCompare(right.username)); unconfirmed.sort((left, right) => left.username.localeCompare(right.username)); rescued.sort((left, right) => left.username.localeCompare(right.username)); const result = { generatedAt: new Date().toISOString(), target, profileCounts: { followers: target.followerCount, following: target.followingCount, }, loaded: { followers: followerLoad.skipped ? "skipped" : followerLoad.usersByUsername.size, following: followingLoad.usersByUsername.size, }, followerListComplete, verificationMode, confirmed, unconfirmed, rescued, unknown, fansNotFollowedBack, authLost, warnings, requestsMade: state.requests, resumedFromPreviousRun: resume.loadedFromStorage, loadPasses: [...followingLoad.passes, ...followerLoad.passes], accuracyNote: "Confirmed = the follower list was read completely (or each miss individually verified), so it is exact. Unconfirmed = the follower list was incomplete and the account could not be proven; it is never counted as a definite non-follower.", }; window.IG_NON_FOLLOWERS_RESULTS = result; state.done = true; progress( `Done: ${confirmed.length} confirmed not following back, ${unconfirmed.length} unconfirmed, ${rescued.length} follow back, ${unknown.length} unknown, ${state.requests} requests.`, "done", ); console.log("IG_NON_FOLLOWERS_SUMMARY", { target: result.target.username, loaded: result.loaded, profileCounts: result.profileCounts, followerListComplete: result.followerListComplete, confirmed: result.confirmed.length, unconfirmed: result.unconfirmed.length, rescued: result.rescued.length, unknown: result.unknown.length, requestsMade: result.requestsMade, warnings: result.warnings, }); renderFinalReport(result); } run().catch((error) => { state.done = true; progress(error.message || String(error), "error"); console.error("Instagram non-follower checker failed", error); }); })();
